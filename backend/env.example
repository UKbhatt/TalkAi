# Database Configuration
# For LOCAL with MongoDB: mongodb://localhost:27017/truegradient
# For LOCAL with Atlas: mongodb+srv://username:password@cluster.mongodb.net/truegradient
# For PRODUCTION: mongodb+srv://username:password@cluster.mongodb.net/truegradient
MONGODB_URI=mongodb://localhost:27017/TalkAi

# JWT Secrets (IMPORTANT: Use strong, unique secrets in production)
JWT_SECRET=dev-jwt-secret-please-change-in-production
JWT_REFRESH_SECRET=dev-jwt-refresh-secret-please-change-in-production

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
# For LOCAL: http://localhost:5173 (Vite default port)
# For PRODUCTION: https://your-frontend-domain.vercel.app (REQUIRED for security)
# IMPORTANT: Set this in production to your actual frontend URL
FRONTEND_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# AI Service (optional - leave empty if not using)
OPENAI_API_KEY=

# Stripe Payment Configuration (optional - leave empty if not using payments)
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_STARTER_PRICE_ID=
STRIPE_PRO_PRICE_ID=
STRIPE_ULTIMATE_PRICE_ID=
